import {useState} from 'react';import {ArrowRight,CheckCircle2,Info,RefreshCw} from 'lucide-react';import {Link,useNavigate} from 'react-router-dom';import UploadZone from '../components/UploadZone';import ModelSelector from '../components/ModelSelector';import LoadingScanner from '../components/LoadingScanner';import {predictCnn,predictYolo} from '../services/api';import type {ModelType} from '../types';
export default function Analyze(){const [image,setImage]=useState<string|null>(null);const [model,setModel]=useState<ModelType>('yolo');const [loading,setLoading]=useState(false);const [error,setError]=useState('');const nav=useNavigate();const run=async()=>{if(!image){setError('Please upload a clear plant image first.');return}setError('');setLoading(true);try{const result=model==='yolo'?await predictYolo(image):await predictCnn(image);sessionStorage.setItem('plantai:lastResult',JSON.stringify({model,result}));nav(model==='yolo'?'/results/yolo':'/results/cnn')}catch{setError('The AI service is unavailable. Please try again.')}finally{setLoading(false)}};return <div className="p-5 lg:p-9"><div className="mb-8"><div className="pill border-[#d8e5d3] bg-white text-[#66806b]">AI Vision Lab</div><h1 className="mt-4 font-display text-3xl font-extrabold tracking-tight text-[#1c3323] sm:text-4xl">Analyze a plant</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-[#78857b]">Upload a leaf or plant photo, choose how you want PlantAI to interpret it, and get a clear health report.</p></div>{loading&&image?<LoadingScanner imageUrl={image}/>:<div className="grid gap-6 xl:grid-cols-[1.1fr_.9fr]"><div className="card p-5"><div className="mb-4 flex items-center justify-between"><div><h2 className="font-display text-lg font-extrabold">1. Upload image</h2><p className="mt-1 text-xs text-[#849087]">Clear, well-lit photos work best.</p></div>{image&&<span className="flex items-center gap-1.5 text-xs font-bold text-[#5c8a61]"><CheckCircle2 size={15}/>Ready</span>}</div><UploadZone imageUrl={image} onImage={setImage} onClear={()=>setImage(null)}/>{error&&<div className="mt-4 rounded-2xl border border-[#f2c8ba] bg-[#fff4ef] px-4 py-3 text-sm font-semibold text-[#9b4c34]">{error}</div>}</div><div className="card p-6"><div><h2 className="font-display text-lg font-extrabold">2. Choose AI model</h2><p className="mt-1 text-xs text-[#849087]">You can compare the same image later.</p></div><div className="mt-5"><ModelSelector value={model} onChange={setModel}/></div><div className="mt-5 rounded-2xl bg-[#f6f8f3] p-4 text-xs leading-5 text-[#718076]"><div className="flex items-start gap-2"><Info size={15} className="mt-0.5 shrink-0 text-[#658e68]"/><span><strong className="text-[#415b47]">{model==='yolo'?'YOLO':'CNN'} selected.</strong> {model==='yolo'?'The demo will highlight localized regions and disease detections.':'The demo will rank the most likely disease classes for the whole image.'}</span></div></div><button onClick={run} className="btn-primary mt-6 w-full text-base">Analyze Image <ArrowRight size={17}/></button><button onClick={()=>setImage(null)} className="btn-secondary mt-3 w-full"><RefreshCw size={16}/>Reset</button><p className="mt-4 text-center text-[11px] leading-5 text-[#8c968e]">Demo mode uses mock AI responses. Connect your backend through <code className="rounded bg-white px-1">src/services/api.ts</code>.</p></div></div>}<div className="mt-6 flex flex-wrap items-center gap-3 text-xs text-[#7c887f]"><span className="font-bold text-[#53675a]">Next:</span><Link to="/compare" className="underline decoration-[#b9cdb4] underline-offset-4">Compare both models</Link><span>•</span><Link to="/assistant" className="underline decoration-[#b9cdb4] underline-offset-4">Ask PlantAI Assistant</Link></div></div>}
