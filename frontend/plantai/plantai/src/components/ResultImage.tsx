import type {Detection} from '../types';
export default function ResultImage({imageUrl,detections}:{imageUrl:string;detections:Detection[]}){return <div className="relative overflow-hidden rounded-[28px] bg-[#172f21]"><img src={imageUrl} alt="Analyzed plant" className="h-[430px] w-full object-cover"/>{detections.map((d,i)=><div key={i} className={`absolute rounded-xl border-2 ${d.status==='diseased'?'border-[#ef9a78]':'border-[#9ce38b]'}`} style={{left:`${d.x}%`,top:`${d.y}%`,width:`${d.width}%`,height:`${d.height}%`}}><span className={`absolute -top-7 left-0 whitespace-nowrap rounded-lg px-2.5 py-1 text-[10px] font-bold text-white ${d.status==='diseased'?'bg-[#b9573a]':'bg-[#3e7c4a]'}`}>{d.label} · {Math.round(d.confidence*100)}%</span></div>)}<div className="absolute bottom-4 left-4 rounded-full bg-[#173b27]/90 px-3 py-1.5 text-xs font-bold text-white backdrop-blur">Live detection overlay</div></div>}
